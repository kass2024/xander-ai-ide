# Legacy overlay — settings are now built into docker-compose.prod.yml (127.0.0.1:8088).
# Safe to include for older deploy scripts; no extra port bindings.
#
#   docker compose -f docker-compose.prod.yml --env-file .env.production up -d
